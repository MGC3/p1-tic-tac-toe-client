'use strict';

const currentPlayer = 'X';

const isValidMove = () => {
  console.log('Hi from isValidMove');
};
const isWinningMove = () => {
  console.log('Hi from isWinningMOve');
};
const togglePlayer = () => {
  console.log('Hi from togglePlayer');
};

module.exports = {
  currentPlayer,
  isValidMove,
  isWinningMove,
  togglePlayer
};
